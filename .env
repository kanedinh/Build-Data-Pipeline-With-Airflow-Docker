AIRFLOW_UID=50000

# Login to the postgres container and run the following command to get the IP address of the container
POSTGRES_USER=root
POSTGRES_PASSWORD=root
POSTGRES_DB=pg_database
POSTGRES_HOST=host.docker.internal
POSTGRES_PORT=5432